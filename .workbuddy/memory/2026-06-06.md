## 2026-06-06 工作记录

### ChatBox 前后端功能对齐检查
- 全面审查了前端 (Tauri/React, `src/` 下 13 个文件) 与 Go 后端 (GitHub: Ps-Student-Catalog-Team/Chatbox-Front, `main.go`) 的协议匹配
- **发现 3 个关键 Bug**:
  1. Token 从未写入 localStorage — 登录后刷新必掉线，所有 HTTP API 401
  2. update_avatar 响应类型不匹配 (`update_avatar_ok` vs `avatar_ok`)
  3. withdraw_message 广播类型不匹配 (`withdraw` vs `withdraw_message`)
- 14 个 HTTP API 和 12+ WebSocket action 已确认匹配

### 修复 3 个关键 Bug (chatStore.tsx)
- **B1**: `auth_ok` handler 添加 `localStorage.setItem('chatbox_token', msg.token!)` 实现 Token 持久化
- **B2**: `update_avatar_ok` handler 增加 `avatar_ok` case，兼容后端实际响应类型
- **B3**: `withdraw` handler 增加 `withdraw_message` case，匹配后端撤回广播类型
- **M1**: `publish_announcement_ok` 添加 `alert('公告已发布')` 反馈
- **M2**: 错误处理器列表添加 `withdraw_message_err`

### 修复头像上传问题 (UserProfile.tsx)
- `handleAvatarUpload` 现在解析后端返回的 `avatar_url`，不再用本地 `URL.createObjectURL(file)`
- 上传成功后调用 `updateAvatar(avatarUrl)` 通过 WebSocket 通知后端同步
- 同时 dispatch `UPDATE_AVATAR` 更新全局状态
- 错误 catch 改为打印 console.error + 显示具体错误信息，方便排查
- 背景图上传同步改善错误日志
- file input 上传后 reset value，允许重新选择同一文件
